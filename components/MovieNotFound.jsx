import React from 'react'

function MovieNotFound({id}) {
  return (
    <div className=' h-screen bg-red-400 text-white'>
      Movie Not found and id is {id}
    </div>
  )
}

export default MovieNotFound
